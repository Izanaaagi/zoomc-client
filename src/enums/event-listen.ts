export enum EventListen {
  UPDATE_PARTICIPANTS = 'updateParticipants',
  RECEIVE_MESSAGE = 'receiveMessage',
  CONNECT = 'connect',
  UPDATE_AVAILABLE_ROOMS = 'updateAvailableRooms',
  ERROR = 'error'
}